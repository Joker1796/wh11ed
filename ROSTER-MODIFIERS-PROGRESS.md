# Roster modifier overlay — план и журнал работ

Заведён 2026-08-19. Закрывает пункт 1 из `ROSTER-BUILDER-PROGRESS.md` («`RosterUnitRulesModal`
не накладывает живые модификаторы»), но вырос в отдельную многофазную задачу, поэтому живёт
своим файлом. Транзиентный: по завершении архитектурная часть уезжает в
`src/components/roster/CLAUDE.md`, а этот файл удаляется (та же судьба, что у
`RECONCILE-912.md` / `APPDATA-COVERAGE-PLAN.md`).

Ветка: `feat/roster-builder`.

---

## 1. Что выяснило исследование (2026-08-19)

**Структурных модификаторов в источнике почти нет.** В `wh40k-appdata` (data version 925):

| Таблица | Строк | Смысл |
| --- | ---: | --- |
| `enhancement` | 961 | правило — **проза** в `localisations.en.rules` |
| `enhancement_datasheet_ability` | **6** | единственная структурная связь enhancement → абилка |
| `enhancement_wargear_item_profile` | **2** | единственная структурная связь enhancement → профиль оружия |
| `detachment` | 292 | — |
| `detachment_rule` | 308 | только имя правила; текст лежит в `rule_container*` |
| `conditional_keyword` | 385 | **структурно**: юнит получает ключевое слово от детачмента/варлорда/аллегиенса |

Иначе говоря: официальное приложение GW **само не пересчитывает статлайны**. И это не лень —
подавляющее большинство модификаторов 40k условные (фаза, дальность, цель, аура, раз за битву).
Показать `T 6` там, где по правилам `T 5`, а `+1` действует только пока активен Waaagh! — это
дезинформация, а не удобство.

**Оценка объёма прозы, которая всё-таки трогает характеристики** (грубый скан регулярками):

- правила детачментов: **46 из 268** блоков `rule:` в `src/data/factions/*.js`
- enhancement'ы: **~155 из 961** (78 «add N to … characteristic», 66 «has the … ability»,
  10 «N+ invulnerable save», 4 «gains the … keyword»)
- армейские правила: почти все условные (Waaagh!, Oath of Moment и т.п.)

**Готовый прецедент в проекте.** `src/data/conditionalKeywords.json` + генератор
`scripts/gen-conditional-keywords.mjs` + проп `grantedKeywords` у `DatasheetCard`:
`FactionDatasheetView` уже показывает ключевое слово, выданное детачментом, в общей строке
keywords — со звёздочкой и сноской «откуда». Философия «не подменяем напечатанное, а
атрибутируем» уже принята в кодовой базе; эта задача распространяет её на статы и оружие.

---

## 2. Принятые решения (2026-08-19, подтверждены пользователем)

1. **Числа.** Безусловный модификатор — применяется к числу и помечается звёздочкой со сноской.
   Условный — **число не трогаем**, рядом висит аннотация («+1 T, пока Waaagh! активен»).
   Это делает поле `when` обязательной частью формата данных яруса C, а не украшением.
2. **Объём первой итерации.** Ярусы **A + B**. Ярус C (числовой сайдкар) — отдельной задачей
   после того, как каркас отрисовки жив.
3. **Источник данных яруса C.** Генератор-черновик по appdata + ручная вычитка пофракционно,
   по образцу `gen-conditional-keywords.mjs`; `scripts/sync.mjs` должен уметь ловить дрейф
   после бампа appdata.
4. **Где показываем.** `RosterUnitRulesModal` (основное) и плитки статов `.rvst` в
   `RosterViewView`'s Units-табе. **Не** в текстовом экспорте (`rosterExport.js` держит
   официальный формат списка) и **не** в передаче в трекер (`rosterHandoff.js` — расширяет
   скоуп и трогает стык с `wh11ed-api`).

---

## 3. Архитектура

### Ярусы

**Ярус A — структурный.** Считается точно из уже имеющихся данных, ручной работы не требует:
- оружие в таблицах датащита фильтруется по фактическому лоадауту entry
  (`entry.wg` + `def.defaults` + `items.js`); логика наполовину есть в
  `rosterEngine.js`'s `defaultLoadoutLines()` / `wargearNames()`
- granted keywords из `conditionalKeywords.json`, но гейтятся **детачментами ростера**,
  а не `useFactionChoice()` (как на странице фракции)
- контекстная шапка: enhancement, флаг варлорда, к кому/кто присоединён

**Ярус B — проза с атрибуцией.** Свёрнутые блоки в модалке, отфильтрованные по применимости
к юниту (гейт по ключевым словам):
- текст выбранного enhancement'а (уже умеет `EnhancementRuleModal`, включая `norm()` для
  расхождения апострофов между roster-данными и faction-данными — переиспользовать, не
  переписывать)
- правило каждого выбранного детачмента
- армейское правило фракции
- абилки присоединённого Leader'а (`entry.leaderOf`)

**Ярус C — числовой сайдкар.** Новая сущность `src/data/rosterModifiers/` (формат ниже),
черновик от генератора, вычитка пофракционно.

### Формат данных яруса C (черновик, к уточнению на старте фазы C)

```js
{
  src: { kind: 'enhancement'|'detachment'|'armyRule', id: 'tuff-git', name: 'Tuff Git' },
  scope: { kws: ['Infantry'], exclKws: [], unitIds: [], bearerOnly: true },
  when: null,               // null = безусловно → применяем к числу
                            // строка-ключ i18n = условие → только аннотация
  effects: [
    { on: 'profile', stat: 't', op: 'add', value: 1 },
    { on: 'melee',   stat: 's', op: 'add', value: 1 },
    { on: 'profile', stat: 'inv', op: 'set', value: '5+' },
  ],
}
```

- `on`: `profile` | `ranged` | `melee`; `stat` — ключи как в датащитах (`m/t/sv/w/ld/oc/inv`,
  оружейные `a/bs/ws/s/ap/d`)
- `op`: `add` | `set` | `improve` (для сейвов — «улучшить на 1», не «прибавить»)
- `when` — **ключ i18n**, а не готовый текст: проект двуязычный, аннотация обязана
  существовать в EN и RU
- статус вычитки (`reviewed: true|false`) хранится в данных, чтобы генератор мог отличить
  новое от уже просмотренного

### Точка подключения

Все три места вызова (`RosterUnitBrowser` — превью при добавлении, `UnitEditorFields` —
«Show datasheet», `RosterViewView` — Units-таб) сегодня передают в `RosterUnitRulesModal`
только `unitId` + `factionSlug`. Понадобится необязательный проп-контекст:

```
:ctx="{ entry, def, items, detachments, units }"
```

`RosterUnitBrowser`'s превью — единственное место **без** entry (юнит ещё не добавлен): там
контекст вырождается до детачментов ростера, и это нормально — granted keywords и правила
детачмента показать можно, enhancement/варгир — нечего. Проп необязательный, отсутствие
контекста должно давать ровно сегодняшнее поведение (базовый датащит).

Расчёт наложения — **чистая функция в `src/composables/rosterModifiers.js`** (без Vue, без
стора, тестируется без монтирования — как весь остальной `roster*.js`), возвращающая
`{ sheet, notes }`: изменённый датащит + список аннотаций с атрибуцией.

---

## 4. Фазы

### Фаза A1 — каркас наложения ✅ СДЕЛАНО (2026-08-19)
- `src/composables/rosterModifiers.js`: `overlaySheet(sheet, ctx)` → `{ sheet, notes }`;
  умеет фильтр оружия по лоадауту, `notes` пока всегда пустой
- проп `ctx` у `RosterUnitRulesModal`, прокинут из `UnitEditorFields` и `RosterViewView`
- `RosterUnitBrowser` **намеренно без `ctx`** (отклонение от «трёх мест вызова»): превью
  показывает юнит, которого в ростере ещё нет, — entry не существует, а печатный лист со всеми
  опциями там как раз и нужен. В фазе B этому месту достанутся детачменты
- 14 тестов на чистую функцию + 3 на модалку (реальные данные Intercessor Squad)
- архитектурная заметка добавлена в `src/components/roster/CLAUDE.md`

### Фаза A2 — granted keywords от детачментов ростера ✅ СДЕЛАНО (2026-08-19)
- `grantedKeywordsFor(unitId, factionSlug, detachments)` в `rosterModifiers.js`; сайдкар
  `conditionalKeywords.json` переиспользован как есть, ничего не генерировали заново
- прокинуто в `DatasheetCard` существующим пропом `grantedKeywords` — нового рендера не
  понадобилось
- `RosterUnitBrowser` теперь **тоже получает `ctx`** (только `detachments`): юнита в ростере ещё
  нет, но детачменты уже применяются

### Фаза A3 — контекстная шапка ✅ СДЕЛАНО (2026-08-19)
- `entryContext(ctx)` → `{ warlord, enhancement, attachedTo }`; чипы над карточкой в модалке,
  рендерятся только когда есть что сказать
- обязательный enhancement (`mandatoryEnhancementFor`) показывается наравне с выбранным
- `attachedTo` резолвится через `leaderTargets`; без списка привязка **не показывается вовсе**,
  а не показывается сырым uid

### Фаза B1 — блоки прозы ⬜
- enhancement (через существующий `norm()`-матчинг), правила детачментов, армейское правило
- гейт по ключевым словам юнита, чтобы не показывать неприменимое
- accordion-стиль как у `DsAccordion` в `collapsible`-режиме карточки

### Фаза B2 — абилки присоединённого Leader'а ⬜
- **осторожно:** слияние Leader+Bodyguard в одну карточку уже пробовали и откатили
  (визуально читалось как вложенность). Здесь — отдельный блок со ссылкой, не merge.

### Фаза C1 — генератор-черновик ⬜
- `scripts/gen-roster-modifiers.mjs` (пишет/обновляет данные) + `--check` (report-only,
  ненулевой выход при незакрытых) — конвенция как у `gen-conditional-keywords.mjs`
- пиннинг хеша источника в каждой записи (см. §5) — это то, что делает обновление
  механическим, а не «перечитать 961 enhancement заново»
- `npm run modifiers:queue` → `MODIFIER-QUEUE.local.json`, симметрично очереди переводов

### Фаза C2 — вычитка пофракционно ⬜
Чек-лист (30 фракций) заполняется по ходу:

```
⬜ adepta-sororitas   ⬜ adeptus-custodes   ⬜ adeptus-mechanicus  ⬜ aeldari
⬜ astra-militarum    ⬜ black-templars     ⬜ blood-angels        ⬜ chaos-daemons
⬜ chaos-knights      ⬜ chaos-space-marines ⬜ chaos-titan-legions ⬜ dark-angels
⬜ death-guard        ⬜ deathwatch         ⬜ drukhari            ⬜ emperors-children
⬜ genestealer-cults  ⬜ grey-knights       ⬜ imperial-agents     ⬜ imperial-knights
⬜ leagues-of-votann  ⬜ necrons            ⬜ orks                ⬜ space-marines
⬜ space-wolves       ⬜ tau-empire         ⬜ thousand-sons       ⬜ titan-legions
⬜ tyranids           ⬜ world-eaters
```

### Фаза C3 — отрисовка чисел ⬜
- звёздочка + сноска на изменённой характеристике в `DatasheetCard`
  (по образцу `.ds-kw-star` / `extraKeywordNotes`)
- те же пометки на плитках `.rvst` в `RosterViewView`
- EN/RU строки аннотаций в `src/i18n/ui.js`

### Фаза C4 — встраивание в процедуру обновления ⬜
Делается **в конце C1**, не раньше: `DATA-SYNC.md` не должен ссылаться на несуществующий
скрипт.
- регистрация проверки в `scripts/sync.mjs` (в шапке-описании и в списке `run(...)`,
  как `condKwStale`)
- `DATA-SYNC.md`: строка в §4 (проход модификаторов), гейт в §6, запись в справочнике «где что
  лежит»
- `package.json`: `modifiers` / `modifiers:check` / `modifiers:queue`
- `.gitignore`: `MODIFIER-QUEUE.local.json` **явной строкой** — шаблон `*.local` его не ловит
  (ровно поэтому `TRANSLATION-QUEUE.local.json` прописан отдельно)

---

## 5. Обновление при выходе новой версии приложения GW

Ярус C — единственный слой этой фичи, который **гниёт молча**: GW правит текст enhancement'а,
а выведенный из него модификатор остаётся старым и продолжает уверенно рисовать `+1 T`.
Ярусы A и B такого класса риска не несут (считаются из свежих данных каждый раз).

Процедура обновления базы уже существует и описана в `DATA-SYNC.md` — **отдельного ритуала
заводить не надо, надо встроиться в существующий**. Ниже — как именно.

### 5.1. Механизм: пиннинг хеша источника

Каждая запись модификатора пиннит источник, из которого выведена:

```js
{
  src: {
    kind: 'enhancement', id: 'tuff-git', name: 'Tuff Git',
    hash: 'a1b2c3d4',      // sha1 нормализованной EN-прозы из appdata, первые 8 символов
    ver: 925,              // data_version, на котором запись вычитана
  },
  reviewed: true,
  effects: [ /* ... */ ],
}
```

Хеш считается по **нормализованной** прозе (через `appdataToMarkup()` + схлопывание пробелов
из `scripts/lib/sync-common.mjs`), чтобы косметика вида `<b>` → `**` не давала ложной
устарелости. Источник хеша — **appdata**, не `src/data/factions/*.js`: appdata канон, а дрейф
нашей прозы против канона и так ловит `sync-faction-text`.

### 5.2. Что печатает `sync-roster-modifiers`

Скрипт классифицирует каждый источник (~961 enhancement + 268 правил детачментов + армейские):

| Статус | Условие | Что делать |
| --- | --- | --- |
| `stale` | хеш записи ≠ хеш свежей прозы | **перечитать правило**, обновить `effects`, переснять хеш |
| `new` | проза проходит эвристику «трогает характеристики», записи нет | вычитать и завести запись |
| `orphan` | запись есть, источник исчез (удалён/переименован) | удалить запись или перепривязать |
| `ok` | хеш совпал | молча |

**Ключевая деталь: запись заводится и на «пустой» результат.** Enhancement, вычитанный и
признанный не дающим числового модификатора, всё равно получает запись с
`effects: []` + хеш — иначе каждый следующий бамп будет заново предлагать его как кандидата.
При этом записи нужны только на **кандидатов и вычитанное**, а не на все 961: правило, никогда
не проходившее эвристику, переоценивается по свежему тексту на каждом прогоне, и если GW
перепишет его в «add 1 to the Toughness characteristic» — оно всплывёт как `new`.

### 5.3. Очередь вычитки (отдельная задача, как перевод)

Симметрично проходу перевода из `DATA-SYNC.md` §4:

```bash
npm run modifiers:queue     # → MODIFIER-QUEUE.local.json (гитигнорен, одноразовый список бампа)
# ручная вычитка: правится src/data/rosterModifiers/<slug>.js
node scripts/gen-roster-modifiers.mjs --done <id>    # переснять хеш + ver, пометить reviewed
npm run modifiers:check     # гейт: ненулевой выход, пока есть stale/new/orphan
```

Для `stale` очередь несёт **старый и новый текст правила** — это ровно та информация, по
которой решение принимается за секунды («поменяли дальность ауры» → модификатор не трогаем;
«было +1 T, стало +1 W» → правим `effects`).

Проход независим от прохода перевода и от EN-правок: элементы очереди не связаны между собой,
так что его можно бить на части. Но **если у модификатора менялся `when`** — надо проверить и
RU-строку условия в `src/i18n/ui.js` (аннотации двуязычные, см. §3).

### 5.4. Куда это встаёт в `DATA-SYNC.md`

| Шаг существующей процедуры | Что добавляется |
| --- | --- |
| §1, `npm run sync` | новая под-проверка `sync-roster-modifiers` в списке `scripts/sync.mjs` + строка в его шапке-описании |
| §2, «сначала план» | статусы `stale`/`new`/`orphan` идут в план бампа наравне с прочими находками |
| §4, проходы | новый проход «4d. Модификаторы ростера» рядом с проходом перевода |
| §6, закрытие | `npm run modifiers:check` добавляется к гейтам (`translate:check`, `parity`, тесты, билд) |
| справочник в конце | строка про `gen-roster-modifiers.mjs` |

Правки в `DATA-SYNC.md` делаются **в фазе C4, после того как скрипт существует** — документ
не должен ссылаться на несуществующую команду.

### 5.5. Перекрёстная проверка с `changes.json`

`../wh40k-appdata/scripts/changes.mjs` уже классифицирует изменения на `CHANGED — prose/errata`
против `CHANGED — scalar`. Наш список `stale` обязан быть **подмножеством** его prose-находок
по enhancement'ам/детачментам. Расхождение — сигнал бага, а не находка:

- у нас `stale`, у `changes.json` тихо → нормализация хеша слишком чувствительна (ловим
  косметику разметки)
- у `changes.json` prose-изменение, у нас `ok` → нормализация съедает значимое (например,
  схлопывает числа или режет хвост правила)

Проверять это стоит на первом же реальном бампе после C1 и записать результат в журнал §8.

---

## 6. Инварианты и грабли (проверять перед правкой)

- **EN↔RU parity.** Любая новая строка — сразу в обеих локалях; `when`-условия хранить
  ключами i18n, а не текстом.
- **`RosterUnitRulesModal` и Teleport.** `FactionAccentScope` живёт **внутри** `.modal-body`,
  не снаружи — второй `<Teleport>` вокруг компонента разрывает CSS-каскад кастомных свойств.
  Не «оптимизировать» это обратно.
- **`.modal-body`'s `:deep(.ds-card)` override на ≤480px** отменяет только escape-to-viewport
  `DatasheetCard`; внутренние bleed'ы трогать нельзя.
- **Лёгкий entry-чанк.** `useRosters.js` не импортирует данные фракций; новый
  `rosterModifiers.js` и его данные обязаны грузиться динамически из редактора/просмотра, а не
  тянуться в корневой бандл (тот же PWA-инвариант, что у `data/roster/index.js`).
- **Имена enhancement'ов не совпадают** между `data/roster/<slug>.js` (из appdata,
  типографские `’`, суффиксы `(Aura)`/`(Upgrade)`) и `data/factions/<slug>.js` (ASCII, флаги
  отдельными полями). Использовать `norm()` из `EnhancementRuleModal.vue`, не изобретать.
- **Детачменты в ростере хранятся именами**, не sid — `migrate()` в `useRosters.js` выкидывает
  uuid-образные остатки.
- После правок данных — `npm run build` и проверка баланса `**`/парити, как требует
  `wh11ed/CLAUDE.md`.

---

## 7. Открытые вопросы

- Формат яруса C выше — черновик; финализировать на старте фазы C1, когда станет видно, какие
  формулировки реально встречаются в 46 + ~155 кандидатах.
- «Improve the Save» и модификаторы сейвов: `sv` и `inv` считаются по-разному (улучшение vs
  установка потолка). Решить, когда дойдём до реальных примеров.
- Нужен ли `reviewed`-статус прямо в данных или отдельным файлом-манифестом — решается на C1.

---

## 8. Журнал

- **2026-08-19.** Исследование источников; выяснено, что структурных модификаторов в appdata
  практически нет (6 + 2 связи на 961 enhancement) и что GW сама статлайны не пересчитывает.
  Найден прецедент `conditionalKeywords.json` + `grantedKeywords` — принят как образец
  философии («атрибутируем, а не подменяем»). Приняты 4 решения из раздела 2. План разложен
  на фазы A1–C3. Кода ещё не написано.
- **2026-08-19 (ярус A закрыт).** A2: сматчить детачменты ростера с сайдкаром оказалось не
  тривиально — сайдкар ключуется id детачмента wh11ed, ростер хранит имя из appdata. Общий
  `slugify()` в лоб даёт **24 совпадения из 25**: у Votann appdata пишет «Dëlve Assault Shift»,
  и `slugify` роняет `ë` → `d-lve-assault-shift` против id `delve-assault-shift`. Тот же класс
  тихого no-op, что был с апострофами в enhancement'ах. Лечится снятием диакритики (NFD +
  выброс combining marks) ПЕРЕД slugify; сам `slugify()` не трогали — он load-bearing для DOM-id
  и поискового индекса. Стало 25/25, кейс закреплён тестом. Альтернативу через
  `sourceIds.json` (мост по uuid) отвергли: файл 747 КБ, тянуть его в роутер-чанк ради
  сопоставления имён — против PWA-инварианта. Проверено, что сайдкар (9.6 КБ) ушёл отдельным
  чанком и в entry не попал. A3: `attachedTo` показывается только когда его реально можно
  разрезолвить в имя. `npx vitest run` 629/629 (было 609), `npm run build` зелёный.
- **2026-08-19 (A1 закрыта).** Реализован каркас наложения. Замеры на реальных данных:
  из 5038 строк оружия по всем 30 фракциям **2000 скрываются** у только что добавленного
  юнита (521 из 1034 юнитов затронуты) — то есть карточка показывает лоадаут, а не каталог
  опций. Два правила безопасности, оба подтверждены замерами и зафиксированы в тестах:
  (1) строка, которую не заявляет ни один предмет варгира, **не скрывается никогда** — таких
  139 из 5038 (2.8%), и спрятать их значило бы удалить оружие, которое у юнита реально есть;
  (2) на мульти-миниатюрных датащитах вычитание не делается вообще, только добавление —
  `rep` там пер-миниатюрный, а единого счёта моделей нет (то же ограничение, что у
  `defaultLoadoutLines()`). Отдельно проверено: пустая таблица стрелкового оружия у свежего
  юнита — **не баг**, у 26 датащитов (Nemesis Dreadknight, Wraithknight with Ghostglaive,
  Canoness with Jump Pack…) дефолтный лоадаут и правда без стрелкового; юнитов, у которых
  исчезло бы всё оружие целиком, ноль. `npx vitest run` 609/609, `npm run build` зелёный.
- **2026-08-19 (продолжение).** Спроектирована процедура обновления под бампы appdata (§5):
  пиннинг хеша источника в каждой записи, классификация `stale`/`new`/`orphan`/`ok`, очередь
  вычитки по образцу очереди переводов, встраивание в существующий `DATA-SYNC.md` вместо
  отдельного ритуала. Кода по-прежнему нет.
