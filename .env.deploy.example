# Copy to .env.deploy (gitignored) and fill in. Sourced by deploy.sh.
# Yandex CDN resource id, purged after each deploy. Empty → purge skipped.
CDN_RESOURCE_ID=
# Optional overrides:
# BUCKET=s3://wh11ed.ru
# AWS_PROFILE=yc
